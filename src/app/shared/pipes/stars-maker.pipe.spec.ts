import { StarsMakerPipe } from './stars-maker.pipe';

describe('StarsMakerPipe', () => {
  it('create an instance', () => {
    const pipe = new StarsMakerPipe();
    expect(pipe).toBeTruthy();
  });
});
